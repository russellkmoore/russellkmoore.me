---
phase: 05-writing-blog-system
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/writing/[...slug].astro
  - src/content/writing/mcp-integration-pattern.md
autonomous: true

must_haves:
  truths:
    - "Visiting /writing/mcp-integration-pattern renders a styled blog post with title, date, tags, and prose content"
    - "Draft posts do not generate pages (404 for draft slugs)"
    - "Post page has full-width gradient hero at top and centered content below"
    - "Code blocks in post content have syntax highlighting with dark theme"
  artifacts:
    - path: "src/pages/writing/[...slug].astro"
      provides: "Dynamic route for individual post pages"
      contains: "getStaticPaths"
    - path: "src/content/writing/mcp-integration-pattern.md"
      provides: "One published post for testing"
      contains: "draft: false"
  key_links:
    - from: "src/pages/writing/[...slug].astro"
      to: "src/layouts/PostLayout.astro"
      via: "import PostLayout"
      pattern: "import PostLayout"
    - from: "src/pages/writing/[...slug].astro"
      to: "astro:content"
      via: "getCollection and render imports"
      pattern: "import.*render.*from.*astro:content"
---

<objective>
Create the dynamic route for individual blog post pages and publish one post for development testing.

Purpose: Individual post pages are the core deliverable of the blog system -- SEO-friendly URLs at /writing/[slug] rendering MDX content with the PostLayout.
Output: Working post pages at /writing/[slug] with one published post visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-CONTEXT.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-RESEARCH.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-01-SUMMARY.md

<interfaces>
<!-- PostLayout accepts frontmatter props -->
From src/layouts/PostLayout.astro (created in Plan 01):
```typescript
interface Props {
  frontmatter: {
    title: string;
    description: string;
    pubDate: Date;
    tags: string[];
  };
}
```

<!-- Astro 5 content collection API -->
```typescript
import { getCollection, render } from 'astro:content';
// getCollection('writing', filterFn) returns CollectionEntry[]
// render(entry) returns { Content, headings }
// entry.id = filename without extension (use for slug)
// entry.data = frontmatter fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dynamic post route and publish one test post</name>
  <read_first>
    - src/layouts/PostLayout.astro
    - src/content/writing/mcp-integration-pattern.md
    - src/content.config.ts
  </read_first>
  <files>src/pages/writing/[...slug].astro, src/content/writing/mcp-integration-pattern.md</files>
  <action>
    1. Create `src/pages/writing/` directory and `[...slug].astro` (per D-03):
       - Import `getCollection` and `render` from `astro:content` (NOT `post.render()` -- Astro 5 API change)
       - Import PostLayout from `../../layouts/PostLayout.astro`
       - `getStaticPaths()`: get all published writing (filter `!data.draft`), map to `{ params: { slug: post.id }, props: { post } }`
       - Use `post.id` NOT `post.slug` (Astro 5 removed slug field)
       - In the template: call `const { Content } = await render(post)` then render `<PostLayout frontmatter={post.data}><Content /></PostLayout>`
       - Add prev/next navigation at bottom: sort all published posts by pubDate descending, find current index, render prev/next links as simple text links with arrows pointing to `/writing/{post.id}`

    2. Update `src/content/writing/mcp-integration-pattern.md`:
       - Set `draft: false` so it renders during development
       - Add real markdown body content for testing (2-3 paragraphs about MCP integration patterns, include a fenced code block with language tag like ```typescript to verify syntax highlighting, include a heading, a list, and a blockquote to test prose styling)
       - Keep existing frontmatter fields (title, description, pubDate, tags)

    AVOID: Do NOT use `post.slug` (undefined in Astro 5). Do NOT call `post.render()` as a method. Do NOT use `[slug].astro` (single bracket) -- use `[...slug].astro` for rest/spread to handle potential nested paths.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/writing/[...slug].astro exists
    - src/pages/writing/[...slug].astro contains `getStaticPaths`
    - src/pages/writing/[...slug].astro contains `import { getCollection, render } from 'astro:content'`
    - src/pages/writing/[...slug].astro contains `post.id` (not post.slug)
    - src/pages/writing/[...slug].astro contains `await render(post)` (not post.render())
    - src/content/writing/mcp-integration-pattern.md contains `draft: false`
    - src/content/writing/mcp-integration-pattern.md contains a fenced code block (```)
    - `npm run build` succeeds and generates /writing/mcp-integration-pattern page
  </acceptance_criteria>
  <done>Individual post pages render at /writing/[slug] URLs. One published post (mcp-integration-pattern) is visible with styled prose content, syntax-highlighted code blocks, and prev/next navigation. Draft posts return 404.</done>
</task>

</tasks>

<verification>
- `npm run build` generates writing/mcp-integration-pattern/index.html
- Build output shows the post page was generated
- No draft posts generate pages
</verification>

<success_criteria>
- /writing/mcp-integration-pattern renders a complete blog post with hero gradient, metadata, and prose content
- Code blocks have syntax highlighting
- Draft posts do not generate pages
- Prev/next navigation renders (even if only one post, gracefully handles single-post case)
</success_criteria>

<output>
After completion, create `.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-02-SUMMARY.md`
</output>
