---
phase: 05-writing-blog-system
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/writing/index.astro
  - src/components/sections/Writing.astro
autonomous: true

must_haves:
  truths:
    - "Visiting /writing shows all published posts sorted by date descending"
    - "Homepage Writing section shows latest 3 published posts as clickable cards"
    - "Homepage Writing section shows 'View all writing' link to /writing"
    - "When no published posts exist, homepage shows 'Coming soon' fallback"
    - "Post cards on both pages link to /writing/[slug] individual post pages"
  artifacts:
    - path: "src/pages/writing/index.astro"
      provides: "Writing index page listing all published posts"
      contains: "getCollection"
    - path: "src/components/sections/Writing.astro"
      provides: "Updated homepage section with latest 3 posts and 'View all writing' link"
      contains: "View all writing"
  key_links:
    - from: "src/pages/writing/index.astro"
      to: "src/components/PostCard.astro"
      via: "import PostCard"
      pattern: "import PostCard"
    - from: "src/components/sections/Writing.astro"
      to: "src/components/PostCard.astro"
      via: "import PostCard"
      pattern: "import PostCard"
    - from: "src/components/sections/Writing.astro"
      to: "/writing"
      via: "View all writing link"
      pattern: "href.*writing"
---

<objective>
Create the /writing index page listing all published posts, and update the homepage Writing section to show the latest 3 posts with a "View all writing" link.

Purpose: Visitors need a way to browse all posts and discover writing from the homepage. These are the two entry points into the blog.
Output: /writing index page and updated homepage Writing section, both using PostCard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-CONTEXT.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-RESEARCH.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-01-SUMMARY.md

@src/components/sections/Writing.astro
@src/components/SectionHeader.astro
@src/pages/index.astro

<interfaces>
<!-- PostCard accepts these props (created in Plan 01) -->
From src/components/PostCard.astro:
```typescript
interface Props {
  title: string;
  description: string;
  pubDate: Date;
  tags: string[];
  href: string;
}
```

<!-- Writing collection entry shape -->
```typescript
// entry.id = filename without extension (e.g., "mcp-integration-pattern")
// entry.data.title, entry.data.description, entry.data.pubDate, entry.data.tags, entry.data.draft
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /writing index page</name>
  <read_first>
    - src/layouts/Layout.astro
    - src/components/PostCard.astro
    - src/components/SectionHeader.astro
  </read_first>
  <files>src/pages/writing/index.astro</files>
  <action>
    Create `src/pages/writing/index.astro` (per D-10, D-11):
    - Import Layout from `../../layouts/Layout.astro`
    - Import PostCard from `../../components/PostCard.astro`
    - Import SectionHeader from `../../components/SectionHeader.astro`
    - Fetch all published posts: `getCollection('writing', ({ data }) => !data.draft)`
    - Sort by pubDate descending: `.sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime())`
    - Wrap in `<Layout title="Writing | Russell Moore" description="Articles on engineering leadership, AI architecture, and building products">`
    - Page structure:
      a. `<section class="py-24">` with `<div class="mx-auto max-w-4xl px-6">`
      b. SectionHeader with title="Writing" subtitle="Thoughts on building"
      c. Vertical list layout (research recommends list over grid for thought leadership): map over sorted posts rendering PostCard for each, with `href={"/writing/" + post.id}` and all data props passed through
      d. Use a vertical stack with `space-y-6` or `flex flex-col gap-6` (single column list, not grid -- per discretion recommendation for professional publication feel)
      e. If no published posts: show "Coming soon." fallback text

    AVOID: Do NOT use grid layout (per discretion -- list is better for text-heavy thought leadership). Do NOT use `post.slug` -- use `post.id`.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/writing/index.astro exists
    - src/pages/writing/index.astro contains `getCollection('writing'`
    - src/pages/writing/index.astro contains `post.id` (not post.slug)
    - src/pages/writing/index.astro imports PostCard
    - src/pages/writing/index.astro imports Layout
    - `npm run build` succeeds and generates writing/index.html
  </acceptance_criteria>
  <done>/writing page renders all published posts in a vertical list sorted by date descending, with each post linking to its individual page</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage Writing section with latest 3 posts</name>
  <read_first>
    - src/components/sections/Writing.astro
    - src/components/PostCard.astro
  </read_first>
  <files>src/components/sections/Writing.astro</files>
  <action>
    Rewrite `src/components/sections/Writing.astro` (per D-07, D-08, D-09, D-11):
    - Keep existing imports: getCollection from astro:content, SectionHeader
    - Add import: PostCard from "../PostCard.astro"
    - Remove import: Card (no longer needed)
    - Fetch published writing (keep existing filter pattern)
    - Sort by pubDate descending and slice to first 3 (per D-07)
    - Keep existing section/div wrapper structure (id="writing", py-24, max-w-4xl)
    - Keep SectionHeader as-is
    - When published posts exist:
      a. Render a `<div class="grid gap-6 md:grid-cols-3">` with PostCard for each of the 3 latest posts (grid for compact homepage display, per D-11 same card design)
      b. Pass props: `title={post.data.title}` `description={post.data.description}` `pubDate={post.data.pubDate}` `tags={post.data.tags}` `href={"/writing/" + post.id}`
      c. Below the grid: "View all writing" link (per D-08): `<a href="/writing" class="mt-8 inline-flex items-center gap-1 text-sm text-accent hover:text-accent-hover transition-colors">View all writing <span aria-hidden="true">&rarr;</span></a>`
    - When no published posts: keep existing "Coming soon." fallback

    AVOID: Do NOT show more than 3 posts. Do NOT use `post.slug`. Do NOT remove the section id="writing" (used for nav anchor scrolling).
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/Writing.astro contains `import PostCard`
    - src/components/sections/Writing.astro contains `.slice(0, 3)`
    - src/components/sections/Writing.astro contains `View all writing`
    - src/components/sections/Writing.astro contains `href="/writing"`
    - src/components/sections/Writing.astro contains `post.id` (not post.slug)
    - src/components/sections/Writing.astro does NOT import Card
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Homepage Writing section shows latest 3 posts as clickable PostCards in a 3-column grid with "View all writing" link to /writing. Falls back to "Coming soon" when no published posts exist.</done>
</task>

</tasks>

<verification>
- `npm run build` generates both writing/index.html and updated index.html
- Homepage section links to /writing
- PostCards on both pages link to individual post pages
</verification>

<success_criteria>
- /writing index page lists all published posts in date-sorted vertical list
- Homepage shows latest 3 posts in a grid with "View all writing" link
- Both pages use PostCard component for consistent rendering
- Clicking any card navigates to /writing/[slug]
- Empty state handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-03-SUMMARY.md`
</output>
