---
phase: 05-writing-blog-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/content.config.ts
  - src/styles/global.css
  - src/layouts/PostLayout.astro
  - src/components/PostCard.astro
autonomous: true

must_haves:
  truths:
    - "MDX files in src/content/writing/ are recognized by the content collection"
    - "Rendered markdown content has proper typography styling (headings, paragraphs, code blocks, links)"
    - "Code blocks render with syntax highlighting on a dark background matching site theme"
    - "PostCard component renders a clickable card with title, date, description, tags, and Read more link"
    - "PostLayout wraps post content with full-width hero gradient, centered content column, and post metadata"
  artifacts:
    - path: "astro.config.mjs"
      provides: "MDX integration and Shiki theme config"
      contains: "mdx()"
    - path: "src/content.config.ts"
      provides: "Updated glob pattern for MDX support"
      contains: "*.{md,mdx}"
    - path: "src/styles/global.css"
      provides: "Typography plugin and prose color overrides"
      contains: "@plugin"
    - path: "src/layouts/PostLayout.astro"
      provides: "Post-specific layout with hero gradient and metadata"
      exports: ["default"]
    - path: "src/components/PostCard.astro"
      provides: "Reusable post card for homepage and index"
      exports: ["default"]
  key_links:
    - from: "src/layouts/PostLayout.astro"
      to: "src/layouts/Layout.astro"
      via: "import and wraps with Layout"
      pattern: "import Layout"
    - from: "src/styles/global.css"
      to: "@tailwindcss/typography"
      via: "@plugin directive"
      pattern: "@plugin.*typography"
---

<objective>
Install MDX infrastructure, configure syntax highlighting, add typography prose styles, and create the PostLayout and PostCard components that all other blog plans depend on.

Purpose: Every blog page (individual posts, index, homepage section) needs MDX rendering, prose typography, and shared components. This plan establishes all foundations in one wave.
Output: Working MDX pipeline with styled prose output, reusable PostCard and PostLayout components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-CONTEXT.md
@.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-RESEARCH.md

@src/layouts/Layout.astro
@src/styles/global.css
@src/content.config.ts
@astro.config.mjs
@src/components/Card.astro

<interfaces>
<!-- Layout.astro accepts title and description props -->
From src/layouts/Layout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
}
```

From src/content.config.ts:
```typescript
// Writing collection schema
z.object({
  title: z.string(),
  description: z.string(),
  pubDate: z.coerce.date(),
  tags: z.array(z.string()).default([]),
  draft: z.boolean().default(true),
})
```

From src/styles/global.css:
```css
@theme {
  --color-background: #0a0a0f;
  --color-foreground: #f5f5f5;
  --color-muted: #a1a1aa;
  --color-accent: #a78bfa;
  --color-accent-hover: #c4b5fd;
  --font-sans: "Geist Variable", "Inter", system-ui, sans-serif;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install MDX + typography, update astro config and content collection</name>
  <read_first>
    - astro.config.mjs
    - src/content.config.ts
    - src/styles/global.css
  </read_first>
  <files>astro.config.mjs, src/content.config.ts, src/styles/global.css, package.json</files>
  <action>
    1. Install dependencies: `npm install @astrojs/mdx @tailwindcss/typography`

    2. Update `astro.config.mjs` (per D-04, D-05):
       - Import `mdx` from `@astrojs/mdx`
       - Add `mdx()` to the integrations array (before sitemap)
       - Add `markdown.shikiConfig.theme: 'github-dark-default'` to the config object

    3. Update `src/content.config.ts` (per D-06, D-12, D-13):
       - Change writing collection glob pattern from `"**/*.md"` to `"**/*.{md,mdx}"`
       - Keep all existing schema fields unchanged

    4. Update `src/styles/global.css` (per D-05):
       - Add `@plugin "@tailwindcss/typography";` after the `@import "tailwindcss"` line
       - Add prose color overrides AFTER the existing @theme block:
         ```css
         /* Prose typography for rendered markdown content */
         .prose {
           --tw-prose-body: #e4e4e7;
           --tw-prose-headings: #f5f5f5;
           --tw-prose-links: #a78bfa;
           --tw-prose-bold: #f5f5f5;
           --tw-prose-code: #e4e4e7;
           --tw-prose-quotes: #a1a1aa;
           --tw-prose-quote-borders: #7c3aed;
           --tw-prose-pre-bg: #0a0a0f;
           --tw-prose-pre-code: #e4e4e7;
         }
         ```

    AVOID: Do NOT add tailwind.config.js (Tailwind v4 uses CSS config). Do NOT use `@import` for the typography plugin -- must be `@plugin`. Do NOT change `@astrojs/tailwind` -- project uses `@tailwindcss/vite` directly.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - astro.config.mjs contains `mdx()` in integrations array
    - astro.config.mjs contains `shikiConfig` with `github-dark-default` theme
    - src/content.config.ts glob pattern is `"**/*.{md,mdx}"`
    - src/styles/global.css contains `@plugin "@tailwindcss/typography"`
    - src/styles/global.css contains `--tw-prose-pre-bg: #0a0a0f`
    - `npm run build` succeeds without errors
  </acceptance_criteria>
  <done>MDX integration installed, Shiki configured with github-dark-default theme, typography plugin loaded via @plugin, prose colors match dark theme, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create PostLayout and PostCard components</name>
  <read_first>
    - src/layouts/Layout.astro
    - src/components/Card.astro
    - src/components/Badge.astro
  </read_first>
  <files>src/layouts/PostLayout.astro, src/components/PostCard.astro</files>
  <action>
    1. Create `src/layouts/PostLayout.astro` (per D-01, D-02):
       - Import and wrap with Layout.astro, passing `title="{frontmatter.title} | Russell Moore"` and `description={frontmatter.description}`
       - Accept props: `frontmatter` object with `{ title: string, description: string, pubDate: Date, tags: string[] }`
       - Override og:type to "article" -- add an `ogType` prop to Layout.astro (update Layout.astro to accept optional `ogType` prop, default "website", use it in the og:type meta tag)
       - Structure (per D-01 full-width immersive layout):
         a. Full-width hero gradient section at top: subtle gradient from a slightly violet-tinted dark color (e.g., rgba(124,58,237,0.08) at top) fading to transparent/background color. Height ~200px.
         b. Inside the gradient area, at the bottom: "Back to Writing" link (anchor to /writing) in text-muted with left arrow
         c. Below gradient: centered content column (max-w-3xl mx-auto px-6)
         d. Post header: pubDate formatted as "Month Day, Year" using toLocaleDateString('en-US', {year:'numeric', month:'long', day:'numeric'}), then h1 title (text-4xl md:text-5xl font-bold), then tags as small accent-colored spans with gap-2 flex-wrap
         e. No reading time (per D-02)
         f. Article body: `<article class="prose prose-invert prose-lg max-w-none">` wrapping a `<slot />`
         g. Bottom: prev/next navigation (optional slot or section -- leave a commented placeholder for Plan 02 to wire)

    2. Create `src/components/PostCard.astro` (per D-09):
       - Accept props: `title: string, description: string, pubDate: Date, tags: string[], href: string`
       - Render as an `<a>` tag wrapping the entire card (entire card is clickable per D-09)
       - Use the same visual style as Card.astro (rounded-lg border border-white/10 bg-white/5 p-6 transition-colors) but add `hover:border-accent/30 hover:bg-white/[0.07]` for interactive feel
       - Inside: pubDate formatted as "Mon DD, YYYY" (short format), title as h3 font-semibold text-foreground, description as text-sm text-muted (line-clamp-2), tags row, and explicit "Read more" text link at bottom with right arrow (per D-09) styled as text-accent text-sm
       - The "Read more" is decorative (the whole card is already the link) -- use a span, not a nested anchor

    3. Update `src/layouts/Layout.astro`:
       - Add optional `ogType` prop (default: "website") to the Props interface
       - Replace hardcoded `<meta property="og:type" content="website" />` with `<meta property="og:type" content={ogType} />`
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/PostLayout.astro exists and imports Layout
    - src/layouts/PostLayout.astro contains "prose prose-invert"
    - src/layouts/PostLayout.astro contains "Back to Writing" or "back" link to /writing
    - src/layouts/PostLayout.astro does NOT contain "reading time" or "min read"
    - src/components/PostCard.astro exists and accepts href prop
    - src/components/PostCard.astro contains "Read more" text
    - src/layouts/Layout.astro contains ogType prop in interface
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>PostLayout renders full-width gradient hero with centered content column and prose-styled article slot. PostCard renders clickable card with title, date, description, tags, and "Read more" link. Layout.astro supports ogType override for article pages.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with MDX integration active
- PostLayout.astro and PostCard.astro exist with correct structure
- global.css has typography plugin and prose overrides
- content.config.ts supports .mdx files
</verification>

<success_criteria>
- MDX pipeline fully configured (integration + Shiki + typography)
- PostLayout provides immersive full-width hero gradient with centered prose content
- PostCard is a reusable clickable card matching D-09 requirements
- All components build without errors
- Foundation is ready for Plan 02 (post pages) and Plan 03 (index + homepage)
</success_criteria>

<output>
After completion, create `.planning/phases/05-writing-blog-system-with-individual-post-pages-writing-index-page-and-homepage-latest-posts-section/05-01-SUMMARY.md`
</output>
