---
phase: 02-components-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/styles/global.css
  - src/components/Card.astro
  - src/components/Badge.astro
  - src/components/SectionHeader.astro
  - src/components/SectionDivider.astro
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/layouts/Layout.astro
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03
  - LAYOUT-04
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "Sticky nav is visible at top of page with name on left and section links on right"
    - "Nav transitions from transparent to solid dark background when scrolling past hero area"
    - "Mobile hamburger menu opens a slide-in panel from the right with section links"
    - "Footer displays copyright, LinkedIn icon, and GitHub icon"
    - "Card, Badge, and SectionHeader components exist and are importable"
    - "Smooth scroll works for anchor links with offset for sticky nav"
  artifacts:
    - path: "src/components/Nav.astro"
      provides: "Sticky navigation with scroll detection and mobile menu"
      contains: "IntersectionObserver"
    - path: "src/components/Footer.astro"
      provides: "Minimal footer with social icons"
      contains: "lucide:linkedin"
    - path: "src/components/Card.astro"
      provides: "Reusable card with default and featured variants"
      exports: "Props interface with variant"
    - path: "src/components/Badge.astro"
      provides: "Tech tag and status badge"
      exports: "Props interface with variant"
    - path: "src/components/SectionHeader.astro"
      provides: "Section title with optional subtitle"
      exports: "Props interface with title and subtitle"
    - path: "src/layouts/Layout.astro"
      provides: "Layout wrapping pages with nav and footer"
      contains: "Nav"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "src/components/Nav.astro"
      via: "import and render above slot"
      pattern: "import Nav"
    - from: "src/layouts/Layout.astro"
      to: "src/components/Footer.astro"
      via: "import and render below slot"
      pattern: "import Footer"
    - from: "src/components/Nav.astro"
      to: "lucide icons"
      via: "astro-icon Icon component"
      pattern: "lucide:menu"
---

<objective>
Install astro-icon, create all reusable components (Card, Badge, SectionHeader, SectionDivider), build the sticky Nav with mobile hamburger menu, build the Footer, and update Layout.astro to wrap content with Nav + Footer. Also add smooth scroll and scroll-padding-top to global.css.

Purpose: Every section component in Plans 02-03 depends on these reusable components and the layout shell. This must land first.
Output: Complete layout shell with nav/footer, all reusable components ready for section composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-components-content/02-CONTEXT.md
@.planning/phases/02-components-content/02-RESEARCH.md
@.planning/phases/01-foundation-scaffolding/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, update configs, create reusable components</name>
  <files>astro.config.mjs, src/styles/global.css, src/components/Card.astro, src/components/Badge.astro, src/components/SectionHeader.astro, src/components/SectionDivider.astro</files>

  <read_first>
    - astro.config.mjs (current config to extend)
    - src/styles/global.css (current tokens)
    - .planning/phases/02-components-content/02-RESEARCH.md (component patterns, astro-icon setup)
    - .planning/phases/01-foundation-scaffolding/01-UI-SPEC.md (spacing, typography, color tokens)
  </read_first>

  <action>
**Step 1: Install astro-icon and lucide icons**
```bash
npm install astro-icon @iconify-json/lucide
```

**Step 2: Update astro.config.mjs** — Add icon() integration:
```javascript
import { defineConfig } from "astro/config";
import tailwindcss from "@tailwindcss/vite";
import sitemap from "@astrojs/sitemap";
import icon from "astro-icon";

export default defineConfig({
  site: "https://russellkmoore.me",
  output: "static",
  integrations: [sitemap(), icon()],
  vite: {
    plugins: [tailwindcss()],
  },
});
```

**Step 3: Update src/styles/global.css** — Add smooth scroll and scroll-padding-top after the existing @theme block:
```css
@import "tailwindcss";

@custom-variant dark (&:where(.dark, .dark *));

@theme {
  --color-background: #0a0a0f;
  --color-foreground: #f5f5f5;
  --color-muted: #a1a1aa;
  --color-accent: #7c3aed;
  --color-accent-hover: #8b5cf6;
  --font-sans: "Geist Variable", "Inter", system-ui, sans-serif;
}

html {
  scroll-behavior: smooth;
  scroll-padding-top: 5rem;
}
```

**Step 4: Create src/components/Card.astro** (per D-09, COMP-01):
- Props interface: `variant?: "default" | "featured"`, `class?: string`
- Base classes: `rounded-lg border border-white/10 bg-white/5 p-6 transition-colors`
- Featured variant adds: `p-8` (more padding)
- Uses `class:list` for conditional classes
- Contains a `<slot />` for content projection

**Step 5: Create src/components/Badge.astro** (COMP-02):
- Props interface: `variant?: "tech" | "status"`, `class?: string`
- Base: `inline-flex items-center rounded px-2 py-0.5 text-xs font-medium`
- Tech variant: `bg-accent/20 text-accent`
- Status variant: `bg-emerald-500/20 text-emerald-400`
- Contains a `<slot />`

**Step 6: Create src/components/SectionHeader.astro** (COMP-03):
- Props interface: `title: string`, `subtitle?: string`
- Markup: `<div class="mb-12">` with `<h2 class="text-2xl font-semibold text-foreground md:text-3xl">` and optional `<p class="mt-2 text-muted">`

**Step 7: Create src/components/SectionDivider.astro** (per D-13):
- No props needed
- Renders a gradient fade divider:
```html
<div class="h-px w-full bg-gradient-to-r from-transparent via-white/10 to-transparent"></div>
```
  </action>

  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - astro.config.mjs contains `import icon from "astro-icon"` and `icon()` in integrations array
    - src/styles/global.css contains `scroll-behavior: smooth` and `scroll-padding-top: 5rem`
    - src/components/Card.astro exists with `interface Props` containing `variant`
    - src/components/Badge.astro exists with `variant?: "tech" | "status"`
    - src/components/SectionHeader.astro exists with `title: string` in Props
    - src/components/SectionDivider.astro exists with `via-white/10`
    - `npm run build` succeeds with zero errors
  </acceptance_criteria>

  <done>astro-icon installed and configured, global.css has smooth scroll, all 4 reusable components exist with typed Props interfaces, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create Nav with sticky scroll + mobile menu, Footer, and update Layout</name>
  <files>src/components/Nav.astro, src/components/Footer.astro, src/layouts/Layout.astro</files>

  <read_first>
    - src/layouts/Layout.astro (current layout to extend)
    - .planning/phases/02-components-content/02-RESEARCH.md (Pattern 3: IntersectionObserver sentinel, Pattern 4: mobile hamburger, astro-icon usage)
    - .planning/phases/02-components-content/02-CONTEXT.md (D-06 sticky nav, D-07 nav links, D-08 smooth scroll, D-15 mobile menu)
    - .planning/phases/01-foundation-scaffolding/01-UI-SPEC.md (color tokens, typography)
  </read_first>

  <action>
**Create src/components/Nav.astro** (LAYOUT-02, D-06, D-07, D-15):

Import `Icon` from `astro-icon/components`.

Structure:
1. A sentinel `<div id="nav-sentinel" class="absolute top-0 h-0"></div>` placed before the nav
2. A `<nav id="site-nav" class="fixed top-0 z-50 w-full transition-colors duration-300">` containing:
   - Inner container: `<div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">`
   - Left: `<a href="#" class="text-lg font-semibold text-foreground">Russell Moore</a>`
   - Right (desktop): `<div class="hidden items-center gap-8 md:flex">` with anchor links for About (#about), Projects (#projects), Career (#career), Writing (#writing), Contact (#contact). Links styled: `text-sm text-muted hover:text-foreground transition-colors`
   - Right (mobile): hamburger button `<button id="menu-toggle" class="md:hidden" aria-label="Toggle menu">` with `<Icon name="lucide:menu" class="size-6 text-foreground" />`

3. Mobile menu overlay `<div id="mobile-menu" class="fixed inset-0 z-40 translate-x-full transition-transform duration-300 md:hidden">`:
   - Backdrop: `<div id="menu-backdrop" class="absolute inset-0 bg-black/50"></div>`
   - Panel: `<div class="absolute right-0 top-0 flex h-full w-64 flex-col bg-background p-6">`
   - Close button with `<Icon name="lucide:x" class="size-6" />`
   - Same 5 section links stacked vertically, `text-lg text-muted hover:text-foreground`
   - Each link has `click` handler that closes menu

4. `<script>` block with:
   - IntersectionObserver watching `#nav-sentinel`. When NOT intersecting, add `bg-background/95 backdrop-blur-sm border-b border-white/10` to nav. When intersecting, remove those classes.
   - Mobile menu open/close functions toggling `translate-x-full` / `translate-x-0` and `document.body.style.overflow`
   - Event listeners on toggle, close, backdrop, and all menu links

**Create src/components/Footer.astro** (LAYOUT-03):

Import `Icon` from `astro-icon/components`.

Structure:
```html
<footer class="border-t border-white/10 py-8">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-6">
    <p class="text-sm text-muted">&copy; {new Date().getFullYear()} Russell Moore</p>
    <div class="flex items-center gap-4">
      <a href="https://linkedin.com/in/russellkmoore" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn" class="text-muted hover:text-foreground transition-colors">
        <Icon name="lucide:linkedin" class="size-5" />
      </a>
      <a href="https://github.com/russellkmoore" target="_blank" rel="noopener noreferrer" aria-label="GitHub" class="text-muted hover:text-foreground transition-colors">
        <Icon name="lucide:github" class="size-5" />
      </a>
    </div>
  </div>
</footer>
```

**Update src/layouts/Layout.astro** (LAYOUT-01):

Add imports for Nav and Footer at the top of the frontmatter:
```astro
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
```

Update body to wrap slot with Nav above and Footer below:
```html
<body class="min-h-screen bg-background font-sans text-foreground antialiased">
  <Nav />
  <main>
    <slot />
  </main>
  <Footer />
</body>
```

Keep all existing head content (FOUC inline style, meta tags, fonts) unchanged.

Per D-14/LAYOUT-04: All responsive behavior is handled via Tailwind responsive modifiers already embedded in Nav (md:flex/md:hidden) and section components in later plans.
  </action>

  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/components/Nav.astro contains `id="nav-sentinel"` and `IntersectionObserver`
    - src/components/Nav.astro contains `id="mobile-menu"` and `translate-x-full`
    - src/components/Nav.astro contains links to `#about`, `#projects`, `#career`, `#writing`, `#contact`
    - src/components/Nav.astro contains `lucide:menu` and `lucide:x` icon references
    - src/components/Footer.astro contains `lucide:linkedin` and `lucide:github`
    - src/components/Footer.astro contains `Russell Moore` copyright text
    - src/layouts/Layout.astro contains `import Nav from` and `import Footer from`
    - src/layouts/Layout.astro contains `<Nav />` before `<slot />` and `<Footer />` after
    - `npm run build` succeeds
  </acceptance_criteria>

  <done>Sticky nav with transparent-to-solid scroll transition, mobile hamburger slide-in menu, footer with social icons, Layout wraps all pages with nav + main + footer</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `npm run dev` shows a page with sticky nav at top (name left, links right)
- On mobile viewport, hamburger icon visible, clicking it slides in menu panel
- Footer visible at bottom with copyright and icon links
- Card, Badge, SectionHeader, SectionDivider components importable in .astro files
</verification>

<success_criteria>
- All 4 reusable components exist with TypeScript Props interfaces
- Nav has IntersectionObserver scroll detection and mobile menu
- Footer renders copyright + LinkedIn + GitHub icons via astro-icon
- Layout.astro wraps slot with Nav and Footer
- Smooth scroll and scroll-padding-top configured in global.css
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-components-content/02-01-SUMMARY.md`
</output>
