---
phase: 03-polish-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Layout.astro
  - src/components/sections/Hero.astro
  - public/robots.txt
autonomous: true
requirements:
  - SEO-01
  - SEO-02
must_haves:
  truths:
    - "Page source contains link rel=canonical with full https URL"
    - "Page source contains og:title, og:description, og:image, og:type, og:url meta tags"
    - "Page source contains twitter:card summary_large_image meta tag"
    - "robots.txt exists with sitemap reference"
    - "Hero gradient animation runs at 10s instead of 20s"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "Canonical URL, OG tags, Twitter Card tags in head"
      contains: "og:title"
    - path: "public/robots.txt"
      provides: "Search engine crawl directives with sitemap reference"
      contains: "Sitemap:"
    - path: "src/components/sections/Hero.astro"
      provides: "Faster mesh gradient animation with increased visibility"
      contains: "meshMove 10s"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "astro.config.mjs"
      via: "Astro.site for canonical and OG image URLs"
      pattern: "new URL.*Astro\\.site"
    - from: "public/robots.txt"
      to: "sitemap-index.xml"
      via: "Sitemap directive"
      pattern: "Sitemap:.*sitemap-index\\.xml"
---

<objective>
Add SEO metadata (canonical URL, Open Graph tags, Twitter Card tags) to Layout.astro head, create robots.txt, and speed up the hero gradient animation.

Purpose: Enable correct page indexing and rich social previews when the URL is shared on LinkedIn/Slack, plus enhance the hero visual per user feedback.
Output: Layout.astro with full SEO head, robots.txt in public/, faster hero animation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-production/03-CONTEXT.md
@.planning/phases/03-polish-production/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/layouts/Layout.astro (current Props interface):
  interface Props { title: string; description?: string; }
  const { title, description = "Russell Moore -- Engineering Leader & Builder" } = Astro.props;

From astro.config.mjs:
  site: "https://russellkmoore.me" (used by Astro.site)

From src/components/sections/Hero.astro (current animation):
  animation: meshMove 20s ease-in-out infinite alternate;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical URL, OG tags, and Twitter Card tags to Layout.astro</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>src/layouts/Layout.astro, astro.config.mjs</read_first>
  <action>
Modify the frontmatter of Layout.astro to derive canonical URL and OG image URL using Astro.site (per D-03):

  const canonicalURL = new URL(Astro.url.pathname, Astro.site);
  const ogImage = new URL("/og-image.png", Astro.site);

Add these elements inside the head, after the existing title and before the link rel="icon":

1. Canonical URL (per D-03):
   &lt;link rel="canonical" href={canonicalURL} /&gt;

2. Open Graph tags (per D-02):
   &lt;meta property="og:title" content={title} /&gt;
   &lt;meta property="og:description" content={description} /&gt;
   &lt;meta property="og:image" content={ogImage} /&gt;
   &lt;meta property="og:type" content="website" /&gt;
   &lt;meta property="og:url" content={canonicalURL} /&gt;

3. Twitter Card tags (per D-02):
   &lt;meta name="twitter:card" content="summary_large_image" /&gt;
   &lt;meta name="twitter:title" content={title} /&gt;
   &lt;meta name="twitter:description" content={description} /&gt;
   &lt;meta name="twitter:image" content={ogImage} /&gt;

Verify title is "Russell Moore -- Engineering Leader & Builder" (per D-04) -- this is set in the page that passes props to Layout, not in Layout itself. Verify the default description is appropriate for social sharing (per D-05) -- the current default works.

Do NOT change the existing Props interface, font imports, Nav/Footer imports, or the is:inline style block. Only add new elements to the head.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && npm run build 2>&1 | tail -5 && grep -c "og:title\|og:description\|og:image\|og:type\|og:url\|twitter:card\|canonical" src/layouts/Layout.astro</automated>
  </verify>
  <acceptance_criteria>
    - grep 'og:title' src/layouts/Layout.astro returns a match
    - grep 'og:description' src/layouts/Layout.astro returns a match
    - grep 'og:image' src/layouts/Layout.astro returns a match
    - grep 'og:type.*website' src/layouts/Layout.astro returns a match
    - grep 'og:url' src/layouts/Layout.astro returns a match
    - grep 'twitter:card.*summary_large_image' src/layouts/Layout.astro returns a match
    - grep 'rel="canonical"' src/layouts/Layout.astro returns a match
    - grep 'new URL.*Astro.site' src/layouts/Layout.astro returns a match
    - npm run build succeeds with exit code 0
  </acceptance_criteria>
  <done>Layout.astro head contains canonical URL, all 5 OG tags, and all 4 Twitter Card tags using Astro.site for absolute URLs. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create robots.txt and speed up hero gradient animation</name>
  <files>public/robots.txt, src/components/sections/Hero.astro</files>
  <read_first>src/components/sections/Hero.astro</read_first>
  <action>
1. Create public/robots.txt with this exact content:

User-agent: *
Allow: /

Sitemap: https://russellkmoore.me/sitemap-index.xml

Note: @astrojs/sitemap generates sitemap-index.xml (not sitemap.xml) as the entry point.

2. Modify Hero.astro gradient animation (per D-06 -- speed up from 20s to 10s, increase blob size and opacity):

In the .hero-gradient::before style block, change:
- First radial-gradient: opacity from 0.15 to 0.20, spread from 50% to 55%
- Second radial-gradient: opacity from 0.08 to 0.12, spread from 50% to 55%
- Third radial-gradient: opacity from 0.05 to 0.08
- Animation duration: from meshMove 20s to meshMove 10s

The updated .hero-gradient::before CSS should be:
  content: "";
  position: absolute;
  inset: -50%;
  background:
    radial-gradient(circle at 30% 40%, rgba(124, 58, 237, 0.20) 0%, transparent 55%),
    radial-gradient(circle at 70% 60%, rgba(59, 130, 246, 0.12) 0%, transparent 55%),
    radial-gradient(circle at 50% 50%, rgba(124, 58, 237, 0.08) 0%, transparent 70%);
  animation: meshMove 10s ease-in-out infinite alternate;
  will-change: transform;

Do NOT change the keyframes, the HTML structure, or the section classes. Only modify the ::before pseudo-element styles.
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && test -f public/robots.txt && grep "sitemap-index.xml" public/robots.txt && grep "meshMove 10s" src/components/sections/Hero.astro && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - public/robots.txt exists
    - grep 'Sitemap:.*sitemap-index.xml' public/robots.txt returns a match
    - grep 'User-agent' public/robots.txt returns a match
    - grep 'meshMove 10s' src/components/sections/Hero.astro returns a match
    - grep '0\.20' src/components/sections/Hero.astro returns a match (increased opacity)
    - grep '0\.12' src/components/sections/Hero.astro returns a match (increased opacity)
    - npm run build succeeds with exit code 0
  </acceptance_criteria>
  <done>robots.txt serves correct crawl directives with sitemap reference. Hero gradient animation runs at 10s with increased blob visibility. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Built HTML contains all OG meta tags with absolute URLs (https://russellkmoore.me/...)
3. robots.txt is in build output (dist/robots.txt)
4. Hero animation CSS shows 10s duration
</verification>

<success_criteria>
- Page source has canonical URL, 5 OG tags, 4 Twitter Card tags
- All URLs in meta tags are absolute (https://russellkmoore.me/...)
- robots.txt exists with correct sitemap reference
- Hero gradient animation duration is 10s with increased opacity values
- Build succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-production/03-01-SUMMARY.md`
</output>
