---
phase: 07-mdx-component-library
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/components/mdx/LinkCard.astro
  - src/components/mdx/index.ts
  - "src/pages/projects/[...slug].astro"
  - "src/pages/writing/[...slug].astro"
autonomous: true
requirements: []
must_haves:
  truths:
    - "LinkCard fetches og:title and og:description at build time and renders a rich preview card"
    - "LinkCard gracefully falls back to styled link with domain name when fetch fails"
    - "All 6 MDX components are auto-registered in project and writing page templates"
    - "MDX files can use Callout, Figure, CodeComparison, ScreenshotCarousel, InteractiveScreenshot, and LinkCard without per-file imports"
    - "npm run build succeeds with all components wired"
  artifacts:
    - path: "src/components/mdx/LinkCard.astro"
      provides: "Build-time metadata fetching link card"
      contains: "fetch"
    - path: "src/components/mdx/index.ts"
      provides: "Complete barrel export of all 6 components"
      contains: "LinkCard"
    - path: "src/pages/projects/[...slug].astro"
      provides: "Auto-registered MDX components for project pages"
      contains: "mdxComponents"
    - path: "src/pages/writing/[...slug].astro"
      provides: "Auto-registered MDX components for writing pages"
      contains: "mdxComponents"
  key_links:
    - from: "src/pages/projects/[...slug].astro"
      to: "src/components/mdx/index.ts"
      via: "import * as mdxComponents"
      pattern: "import.*mdxComponents.*components/mdx"
    - from: "src/pages/writing/[...slug].astro"
      to: "src/components/mdx/index.ts"
      via: "import * as mdxComponents"
      pattern: "import.*mdxComponents.*components/mdx"
    - from: "src/pages/projects/[...slug].astro"
      to: "Content component"
      via: "components prop"
      pattern: "components=.*mdxComponents"
    - from: "src/components/mdx/LinkCard.astro"
      to: "External URLs"
      via: "build-time fetch with AbortSignal.timeout"
      pattern: "AbortSignal.timeout"
---

<objective>
Build LinkCard (the most complex component -- build-time metadata fetching), complete the barrel export with all 6 components, and wire auto-registration into both project and writing page templates via the Content components prop.

Purpose: Completes the MDX component library and makes all components available in every MDX file without explicit imports.
Output: LinkCard component, complete barrel file, auto-registration in page templates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mdx-component-library-screenshotcarousel-interactivescreenshot-callout-codecomparison-figure-and-linkcard-for-use-in-writing-and-project-pages/07-CONTEXT.md
@.planning/phases/07-mdx-component-library-screenshotcarousel-interactivescreenshot-callout-codecomparison-figure-and-linkcard-for-use-in-writing-and-project-pages/07-RESEARCH.md
@.planning/phases/07-mdx-component-library-screenshotcarousel-interactivescreenshot-callout-codecomparison-figure-and-linkcard-for-use-in-writing-and-project-pages/07-01-SUMMARY.md
@.planning/phases/07-mdx-component-library-screenshotcarousel-interactivescreenshot-callout-codecomparison-figure-and-linkcard-for-use-in-writing-and-project-pages/07-02-SUMMARY.md

<interfaces>
<!-- Current page templates that need modification -->

From src/pages/projects/[...slug].astro -- the frontmatter script contains:
  const { project, prevProject, nextProject } = Astro.props;
  const { Content } = await render(project);

And the template renders:
  <ProjectLayout frontmatter={project.data}>
    <Content />
  </ProjectLayout>

From src/pages/writing/[...slug].astro -- the frontmatter script contains:
  const { Content } = await render(post);

And the template renders:
  <PostLayout frontmatter={post.data}>
    <Content />
  </PostLayout>

From src/components/mdx/index.ts (after plan 01+02, expected state):
  export { default as Callout } from "./Callout.astro";
  export { default as Figure } from "./Figure.astro";
  export { default as CodeComparison } from "./CodeComparison.astro";
  // ScreenshotCarousel and InteractiveScreenshot added by plan 02
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LinkCard and finalize barrel export</name>
  <files>src/components/mdx/LinkCard.astro, src/components/mdx/index.ts</files>
  <read_first>
    - src/components/mdx/index.ts (current barrel exports from plans 01+02)
    - src/components/Card.astro (card styling pattern)
    - src/styles/global.css (theme tokens)
  </read_first>
  <action>
**LinkCard.astro (per D-18, D-19, D-20, D-21, D-22, D-23):**

Props interface: `{ href: string }`

**Frontmatter script (build-time fetch per D-19):**
- Extract domain from URL via `new URL(href).hostname`
- Fetch the URL with User-Agent header and `AbortSignal.timeout(5000)`
- Parse HTML response text with regex to extract:
  - og:title or fallback to title tag
  - og:description or fallback to meta description
- Wrap ALL fetch logic in try/catch -- on failure, set title to domain name, description to empty
- This runs at build time because Astro component frontmatter executes during static build

**Template:**
- Wrap in `<a href={href} target="_blank" rel="noopener noreferrer" class="...">` styled as a card
- Card styling: `block my-4 rounded-lg border border-white/10 bg-white/5 p-4 no-underline transition-colors hover:border-accent/50 hover:bg-white/8 not-prose group`
- Title: `<span class="block font-semibold text-foreground group-hover:text-accent transition-colors">{title}</span>`
- Description (if exists): `<span class="mt-1 block text-sm text-muted line-clamp-2">{description}</span>`
- Domain: `<span class="mt-2 block text-xs text-muted/70">{domain}</span>` with external link icon
- Fallback behavior (per D-20): If fetch fails, title defaults to domain, description is empty -- card still renders as a styled link showing the domain name
- Zero JS -- all fetching happens at build time in frontmatter

**Update barrel export (src/components/mdx/index.ts):**
Add all 6 exports. Read the current file first, then write the complete version with exports for: Callout, Figure, CodeComparison, ScreenshotCarousel, InteractiveScreenshot, LinkCard
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && test -f src/components/mdx/LinkCard.astro && grep -q "AbortSignal.timeout" src/components/mdx/LinkCard.astro && grep -q "og:title" src/components/mdx/LinkCard.astro && grep -c "export" src/components/mdx/index.ts | grep -q "6" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/mdx/LinkCard.astro exists with Props interface accepting href
    - LinkCard fetches URL metadata at build time using fetch() in frontmatter per D-19
    - LinkCard uses AbortSignal.timeout(5000) for timeout protection
    - LinkCard extracts og:title and og:description with regex
    - LinkCard has try/catch with graceful fallback per D-20
    - LinkCard renders as styled anchor tag with title, description, domain
    - src/components/mdx/index.ts exports all 6 components (Callout, Figure, CodeComparison, ScreenshotCarousel, InteractiveScreenshot, LinkCard)
  </acceptance_criteria>
  <done>LinkCard fetches og metadata at build time and renders rich preview card with graceful fallback. Barrel file exports all 6 components.</done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-registration into page templates</name>
  <files>src/pages/projects/[...slug].astro, src/pages/writing/[...slug].astro</files>
  <read_first>
    - src/pages/projects/[...slug].astro (current template)
    - src/pages/writing/[...slug].astro (current template)
    - src/components/mdx/index.ts (barrel file to import from)
  </read_first>
  <action>
**Both page templates need the same change -- pass components prop to Content.**

**src/pages/projects/[...slug].astro:**
1. Add import after existing imports: `import * as mdxComponents from "../../components/mdx";`
2. Change `<Content />` to `<Content components={mdxComponents} />`
3. Leave all other code (getStaticPaths, prev/next nav) untouched

**src/pages/writing/[...slug].astro:**
1. Add import after existing imports: `import * as mdxComponents from "../../components/mdx";`
2. Change `<Content />` to `<Content components={mdxComponents} />`
3. Leave all other code (getStaticPaths, share buttons, related posts, prev/next nav) untouched

This makes all 6 MDX components available in every .mdx file without per-file imports. Authors just write `<Callout>`, `<Figure>`, etc. directly in their MDX content.

**Verify with build:** Run `npm run build` to confirm:
- All existing project and writing pages still build correctly
- No type errors from the components prop
- No regression in existing MDX content rendering
  </action>
  <verify>
    <automated>cd /Users/rmoore/Workspaces/russellkmoore.me && grep -q "mdxComponents" src/pages/projects/\[...slug\].astro && grep -q "mdxComponents" src/pages/writing/\[...slug\].astro && grep -q "components=" src/pages/projects/\[...slug\].astro && grep -q "components=" src/pages/writing/\[...slug\].astro && npm run build 2>&1 | tail -3 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/projects/[...slug].astro imports mdxComponents from ../../components/mdx
    - src/pages/projects/[...slug].astro passes components={mdxComponents} to Content
    - src/pages/writing/[...slug].astro imports mdxComponents from ../../components/mdx
    - src/pages/writing/[...slug].astro passes components={mdxComponents} to Content
    - npm run build succeeds with no errors
    - Existing page content renders correctly (no regression)
  </acceptance_criteria>
  <done>All 6 MDX components auto-registered in both project and writing page templates. Build passes. MDX authors can use components without imports.</done>
</task>

</tasks>

<verification>
- LinkCard exists with build-time fetch and fallback
- Barrel file exports all 6 components
- Both page templates import and pass mdxComponents
- `npm run build` succeeds end-to-end
- No regressions in existing content
</verification>

<success_criteria>
- LinkCard fetches metadata at build time with 5s timeout and graceful fallback
- Complete barrel export of all 6 MDX components
- Auto-registration wired into projects and writing page templates
- Full build succeeds with all components available in MDX
</success_criteria>

<output>
After completion, create `.planning/phases/07-mdx-component-library-screenshotcarousel-interactivescreenshot-callout-codecomparison-figure-and-linkcard-for-use-in-writing-and-project-pages/07-03-SUMMARY.md`
</output>
